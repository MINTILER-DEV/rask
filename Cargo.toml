[package]
name = "rask"
version = "0.1.0"
edition = "2021"
description = "Rask language prototype"
license = "MIT"

[dependencies]
serde_json = "1.0"
unicode-segmentation = "1.11"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
sha2 = "0.10"
